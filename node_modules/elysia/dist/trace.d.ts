import type { TraceHandler, TraceReporter, TraceStream } from './types';
export declare const createTraceListener: (reporter: TraceReporter, handler: TraceHandler<any, any>) => (event: TraceStream) => Promise<void>;
